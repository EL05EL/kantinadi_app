name: kantinadi_app
description: Aplikasi Digitalisasi Kantin UAD 4
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6
  url_launcher: ^6.2.5
  http: ^1.1.0
  supabase_flutter: ^2.0.0
  shared_preferences: ^2.2.2
  flutter_dotenv: ^5.1.0
  pdf: ^3.10.7
  share_plus: ^7.2.1
  path_provider: ^2.1.1
  mobile_scanner: ^3.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logokantinadiapp.png
    - .env